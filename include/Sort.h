#ifndef SORT_H
#define SORT_H

#include <vector>
#include <string>
#include <iostream>
#include <fstream>

class Sort {
  public:
    Sort();
    ~Sort();

    static void insertion(std::vector<int> arr);
};

#endif
